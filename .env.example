# Copy to `.env` (gitignored) and fill in. The zone server reads these from the environment.
#
# Supabase service_role key for project reaiolskmzorymnrbtab — lets the SERVER write the
# inventory table (loot + equip), which clients are no longer allowed to write directly.
# Dashboard → Settings → API → service_role. SECRET — never commit it, never ship it to clients.
SUPABASE_SERVICE_KEY=
