# Candidate model staging

This directory holds generated model candidates before any asset is approved, optimized, or integrated into the game.

- `batches/` contains generation provenance and raw review candidates.
- Nothing in this directory is a runtime asset.
- Moving a model into `models/` requires a separate review and implementation step.
- Meshy API keys and signed authorization data must never be stored here.

